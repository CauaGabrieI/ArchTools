# Rollback e uninstall

Antes de restaurar, o programa mostra a contagem de backups e pacotes registrados. Apenas pacotes de `installed-packages.txt` são candidatos à remoção. Pacotes já existentes ficam em `existing-packages.txt` e nunca entram no uninstall. Serviços guardam seu estado anterior em `services.txt`.
